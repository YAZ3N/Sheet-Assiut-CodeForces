#include <iostream>
using namespace std;
int main() {
 
	int n;
	cin >> n;
	int counter = 0;
	for (int i = 1; i <= n; i++) {
		if (i % 2 == 0) {
			cout << i<<endl;
			counter++;
		}
	}
	if (counter == 0) {
		cout << -1;
	}
}
