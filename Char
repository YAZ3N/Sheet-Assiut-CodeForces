#include <iostream>
using namespace std;
int main()
{
	char op;
	cin >> op;
	if (op >= 97 && op <= 122) {
		cout << char (op - 32);
	}
	else if (op >= 65 && op<= 90) {
		cout << char (op + 32);
	}
	
}
